{
  "name": "conductengine",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build:ecs": "yarn --cwd packages/conduct-ecs build",
    "build:renderer": "yarn --cwd packages/conduct-renderer build",
    "build:demo": "yarn build:ecs && yarn build:renderer && yarn --cwd packages/demos dev"
  },
  "dependencies": {
    "reflect-metadata": "^0.2.1",
    "typescript": "^5.3.3"
  },
  "devDependencies": {
    "@webgpu/types": "^0.1.47",
    "prettier": "3.1.1"
  },
  "workspaces": [
    "packages/*"
  ]
}
